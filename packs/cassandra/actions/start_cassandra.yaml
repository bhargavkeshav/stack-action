---
name: start_cassandra
runner_type: remote-shell-cmd
description: Start DSE cassandra service.
enabled: true
entry_point: ''
parameters:
  sudo:
    default: true
  cmd:
    default: "service cassandra start"
